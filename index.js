import React from "react";
import  ReactDOM  from "react-dom";

import App from './App';

// ReactDOM.render(what to show(like what I want to show), where to show(where I want to show))
ReactDOM.render(<App />, document.getElementById('root'));